server{
    listen 3000;
    server_name front-end;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # proxy_pass configuration
    location /server {
        proxy_pass http://back-end:6556;
    }
}
